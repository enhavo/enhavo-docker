name: Continuous Integration

on:
    pull_request:
    push:
      branches:
        - main

jobs:
    build:
        runs-on: ubuntu-24.04
        name: "Build All"
        steps:
            - uses: actions/checkout@v1

            - name: Build all
              run: make build-all
